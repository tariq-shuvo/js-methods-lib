/*
This method determines whether the
passed value is an array or not.
*/
const arr = ["banana", "apple", "cherry"]

console.log(Array.isArray(arr));

const isArrayInfo = Array.isArray(arr)

console.log(Array.isArray(isArrayInfo));