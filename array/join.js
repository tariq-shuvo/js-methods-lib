/*
This method joins all the elements of an
array into a string using a specified separator.
It takes one optional argument: the separator
to use.
*/

const car = ["BMW", "Tyota", "Honda", "Mazda", "Mitshubishi", "TATA"]
const str = car.join(", ")

console.log(str);