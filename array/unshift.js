/*
method adds one or more elements to
the beginning of an array and returns the new
length of the array.
*/

const arr = ["banana", "apple", "cherry"]

arr.unshift("grape", "lichy")

console.log(arr);